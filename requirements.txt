# Requirements for Universal XBLA Extraction Tool
# Install with: pip install -r requirements.txt

alive-progress>=3.3.0
patool>=4.0.0

# Optional but recommended for better file type detection
python-magic>=0.4.27
python-magic-bin>=0.4.14